schema: spec-driven

context: |
  Tech stack: C++11, Qt5 (Widgets + Network), libpcap, libnet
  Platform: Linux (uses /proc/net/arp, /proc/net/route, POSIX sockets)
  Build system: qmake
  Domain: Network security tool - packet sniffer and ARP/ICMP/Smurf attack utilities
  Naming: Class names PascalCase, methods camelCase, member variables m_ prefix
